// Navbar do dashboard autenticado
export function Navbar() {
  return (
    <nav className="flex items-center justify-between px-6 h-full">
      {/* Logo, org switcher, user menu */}
    </nav>
  )
}